REACT_APP_API_URL=https://localhost:7050/api

# Works around a known react-scripts + eslint-plugin-jest version-conflict
# bug that can appear after a fresh npm install. Safe to keep as-is.
DISABLE_ESLINT_PLUGIN=true
